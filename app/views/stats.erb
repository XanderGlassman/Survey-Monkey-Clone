<!-- < @surveys.each do |survey| >%% 
		 		<if survey.id == session[:survey] >%%
		 			<h1>< survey.name >%%=</h1>
					puts the stats here plugins or whatver
		 		<end>%%
		 < end >%%
-->

<h1>Home Link 	<a href="/users/<%= session[:user_id] %>/index">Home</a></h1>